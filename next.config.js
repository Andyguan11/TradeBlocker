/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // If you're not using a 'src' directory, you might need to specify the directory where your pages are located
  // For example:
  // pageExtensions: ['js', 'jsx', 'ts', 'tsx'],
  // dir: './',
}

module.exports = nextConfig
